# Networking

Pi-side networking is provided by the Amiga net device under `platforms/amiga/net/`.

See:
- `platforms/amiga/net/readme.md`

This device pairs with host services for packet handling. Ensure the kernel module is loaded and `/dev/pistorm` is accessible.
