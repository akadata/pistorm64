# Janus Bus Engine (experimental)

This repo focuses on the PiStorm64 emulator and Amiga platform glue. The Janus Bus Engine work is treated as experimental and may live in separate tooling or branches.

Use this page to record integration notes:

- What Janus side changes were required.
- How Janus bus slots are mapped into Amiga address space.
- Any autoconfig device IDs used for the Janus bus.
- Host-side services (if any) that depend on Janus messages.

If the Janus engine is being maintained in another repo (e.g. `$HOME/janus-misc`), document the exact commit and the build/install steps here.
