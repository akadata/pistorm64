# PISSL Amiga Tooling

This directory builds:

- `libpissl.a` (placeholder)
- `C:pissl` + `C:pissl020`, `C:pissl030`, `C:pissl040`

Current CLI usage:

```
pissl
```

It prints a stub message. TLS is not implemented yet.

Build:
```
make
```

Install into `/opt/amiga`:
```
make install
```
