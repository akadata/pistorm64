dos.library/CompareDates ]" border=0> NAME CompareDates -- Compares two datestamps (V36) SYNOPSIS result = CompareDates(date1,date2) D0 D1 D2 LONG CompareDates(struct DateStamp *,struct DateStamp *) FUNCTION Compares two times for relative magnitide. 0 if date2 is later than date1. NOTE: this is NOT the same ordering as strcmp! INPUTS date1, date2 - DateStamps to compare RESULT result - 0 based on comparison of two date stamps SEE ALSO DateStamp() , DateToStr() , StrToDate() [Back to Amiga Developer Docs ]