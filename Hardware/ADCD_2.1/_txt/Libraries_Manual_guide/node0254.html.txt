SOURCE: Libraries_Manual_guide/node0254.html
TITLE: 15 / / Functions for GadTools Menus / Freeing Menus

15 / / Functions for GadTools Menus / Freeing Menus ]" border=0> The FreeMenus() function frees all the memory allocated by the corresponding call to CreateMenus() . void FreeMenus( struct Menu *menu ); Its one argument is the Menu or MenuItem pointer that was returned by CreateMenus() . It is safe to call FreeMenus() with a NULL parameter, the function will then return immediately. [Back to Amiga Developer Docs ]
