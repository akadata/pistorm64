10 / Example Printer Driver Source Code / EpsonX ]" border=0> For the EpsonX driver, both the assembly and C version of Transfer() are supplied. In the Makefile the (faster) assembly version is used to generate the driver. The EpsonX driver can be generated with the included Makefile. Makefile init.asm transfer.asm macros.i data.c transfer.c printertag.asm dospecial.c density.c rev.i render.c [Back to Amiga Developer Docs ]