# PiStorm64 module defaults.
# Keep gpclk_src=5. Tested/stable range currently observed: gpclk_div=4..6.
# Default to div=4 for maximum tested speed on current RTL, with bus arbitration enabled.
options pistorm gpclk_src=5 gpclk_div=4 run_batch_enable=1 berr_reset_input=1 bus_arb_release=1
